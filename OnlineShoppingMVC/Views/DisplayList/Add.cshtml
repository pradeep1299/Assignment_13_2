@model OnlineShoppingMVC_Entity.ProductDetails

@{
    ViewBag.Title = "Add";
}

<h2>Add Product</h2>

@using (Html.BeginForm("Add", "DisplayList", FormMethod.Post))
{
    <table cellpadding="10">
        <tr>
            <td>
                @Html.Label("ProductId")
            </td>
            <td>
                @Html.TextBox("productId")
            </td>
        </tr>
        <tr>
            <td>@Html.LabelFor(Model => Model.productName)</td>
            <td>@Html.TextBox("productName")
            </td>
        </tr>
        <tr>
            <td>@Html.LabelFor(Model => Model.productPrice)</td>
            <td>@Html.TextBox("productPrice")
            </td>
        </tr>
        <tr>
            <td>
                @Html.ActionLink("Back", "Index")
            </td>
            <td>
                <input type="submit" id="Add" />
            </td>
        </tr>
    </table>
}

